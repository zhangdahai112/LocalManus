# 沙箱迁移指南

<cite>
**本文档中引用的文件**
- [FIRECRACKER_TROUBLESHOOTING.md](file://localmanus-backend/scripts/FIRECRACKER_TROUBLESHOOTING.md)
- [firecracker_sandbox.py](file://localmanus-backend/core/firecracker_sandbox.py)
- [sandbox.py](file://localmanus-backend/core/sandbox.py)
- [test_sandbox.py](file://localmanus-backend/scripts/test_sandbox.py)
- [config.py](file://localmanus-backend/core/config.py)
- [.env.example](file://localmanus-backend/.env.example)
- [gen_web.py](file://localmanus-backend/skills/gen-web/gen_web.py)
- [system_tools.py](file://localmanus-backend/skills/system-execution/system_tools.py)
- [skill_manager.py](file://localmanus-backend/core/skill_manager.py)
- [main.py](file://localmanus-backend/main.py)
- [orchestrator.py](file://localmanus-backend/core/orchestrator.py)
- [agent_manager.py](file://localmanus-backend/core/agent_manager.py)
</cite>

## 更新摘要
**所做更改**
- 更新文档标题以反映文件重命名：从 SANDBOX_MIGRATION_GUIDE.md 到 FIRECRACKER_TROUBLESHOOTING.md
- 更新所有文件引用以使用新的文件名
- 更新架构图以反映新的 agent-infra/sandbox 架构
- 更新故障排除部分以包含新的故障排除指南
- 更新配置管理部分以反映新的环境变量
- 更新技能集成示例以使用新的沙箱 API

## 目录
1. [简介](#简介)
2. [项目结构概览](#项目结构概览)
3. [核心组件分析](#核心组件分析)
4. [架构对比](#架构对比)
5. [迁移步骤详解](#迁移步骤详解)
6. [配置管理](#配置管理)
7. [技能集成](#技能集成)
8. [测试与验证](#测试与验证)
9. [故障排除](#故障排除)
10. [最佳实践](#最佳实践)
11. [总结](#总结)

## 简介

LocalManus 项目已成功从传统的 Firecracker 虚拟机沙箱系统迁移到基于 [agent-infra/sandbox](https://github.com/agent-infra/sandbox) 的现代化沙箱架构。新系统提供了更强大的功能、更好的易用性和更高的灵活性，支持本地共享模式和在线隔离模式两种运行方式。

新沙箱系统的主要优势包括：
- **浏览器自动化** - 完整的 Playwright/CDP 支持和 VNC 访问
- **开发环境** - VSCode Server、Jupyter、终端等完整开发工具
- **文件操作** - 完整的文件系统访问能力
- **MCP 集成** - 模型上下文协议服务器支持
- **双模式运行** - 本地（共享）或在线（隔离）容器模式

**章节来源**
- [FIRECRACKER_TROUBLESHOOTING.md](file://localmanus-backend/scripts/FIRECRACKER_TROUBLESHOOTING.md#L1-L12)

## 项目结构概览

```mermaid
graph TB
subgraph "后端核心"
A[main.py<br/>主应用入口]
B[orchestrator.py<br/>编排器]
C[agent_manager.py<br/>代理管理器]
D[config.py<br/>配置管理]
end
subgraph "沙箱系统"
E[firecracker_sandbox.py<br/>沙箱管理器]
F[sandbox.py<br/>兼容性适配器]
G[test_sandbox.py<br/>测试脚本]
end
subgraph "技能系统"
H[gen_web.py<br/>Web 项目生成技能]
I[system_tools.py<br/>系统执行技能]
J[skill_manager.py<br/>技能管理器]
end
subgraph "配置文件"
K[.env.example<br/>环境变量示例]
L[FIRECRACKER_TROUBLESHOOTING.md<br/>故障排除指南]
end
A --> B
B --> C
C --> J
J --> H
J --> I
H --> E
I --> E
E --> F
D --> E
K --> D
```

**图表来源**
- [main.py](file://localmanus-backend/main.py#L1-L50)
- [firecracker_sandbox.py](file://localmanus-backend/core/firecracker_sandbox.py#L1-L50)
- [gen_web.py](file://localmanus-backend/skills/gen-web/gen_web.py#L1-L30)

**章节来源**
- [main.py](file://localmanus-backend/main.py#L1-L100)
- [firecracker_sandbox.py](file://localmanus-backend/core/firecracker_sandbox.py#L1-L100)

## 核心组件分析

### 沙箱管理器 (SandboxManager)

沙箱管理器是整个系统的核心组件，负责管理用户沙箱实例的生命周期和资源分配。

```mermaid
classDiagram
class SandboxMode {
<<enumeration>>
LOCAL
ONLINE
}
class SandboxInfo {
+string sandbox_id
+string base_url
+SandboxMode mode
+string container_id
+string vnc_url
+string vscode_url
+string home_dir
}
class SandboxClient {
+string base_url
+int timeout
+Session session
+get_context() Dict
+exec_command(command, cwd) Dict
+read_file(path) string
+write_file(path, content) Dict
+list_files(path) List
+screenshot() bytes
+get_browser_info() Dict
+execute_jupyter_code(code) Dict
}
class SandboxManager {
+SandboxMode mode
+string local_url
+bool use_china_mirror
+Dict~string,SandboxInfo~ sandboxes
+get_sandbox(user_id) SandboxInfo
+get_client(user_id) SandboxClient
+execute_command(user_id, cmd, cwd) Dict
+cleanup_sandbox(user_id) void
+cleanup_all() void
}
SandboxManager --> SandboxInfo : 创建
SandboxManager --> SandboxClient : 使用
SandboxClient --> SandboxInfo : 依赖
```

**图表来源**
- [firecracker_sandbox.py](file://localmanus-backend/core/firecracker_sandbox.py#L15-L120)

### 兼容性适配器

为了保持向后兼容性，系统提供了 LegacySandboxAdapter 来适配旧版本的返回格式。

```mermaid
classDiagram
class LegacySandboxAdapter {
+create_sandbox(user_id) string
+execute_command(user_id, command, cwd) Dict
}
class SandboxManager {
+get_sandbox(user_id) SandboxInfo
+execute_command(user_id, cmd, cwd) Dict
}
class legacy_sandbox {
<<global instance>>
}
LegacySandboxAdapter --> SandboxManager : 委托
legacy_sandbox --> LegacySandboxAdapter : 实例化
```

**图表来源**
- [sandbox.py](file://localmanus-backend/core/sandbox.py#L11-L40)

**章节来源**
- [firecracker_sandbox.py](file://localmanus-backend/core/firecracker_sandbox.py#L103-L294)
- [sandbox.py](file://localmanus-backend/core/sandbox.py#L1-L46)

## 架构对比

### 传统 Firecracker 架构

```mermaid
flowchart LR
A[FirecrackerManager] --> B[VM Process]
B --> C[Unix Socket]
C --> D[Firecracker API]
subgraph "限制"
E[内核编译需求]
F[rootfs 配置复杂]
G[Socket 通信限制]
H[功能相对简单]
end
D -.-> E
D -.-> F
D -.-> G
D -.-> H
```

### 新版 agent-infra/sandbox 架构

```mermaid
flowchart TD
subgraph "LOCAL 模式"
A[SandboxManager] --> B[HTTP API]
B --> C[共享沙箱]
C --> D[VNC 访问]
C --> E[VSCode Server]
C --> F[浏览器自动化]
end
subgraph "ONLINE 模式"
A --> G[Docker 容器]
G --> H[隔离沙箱]
H --> I[独立资源]
H --> J[完全隔离]
end
subgraph "统一接口"
K[SandboxClient]
L[SandboxInfo]
M[SandboxMode]
end
A --> K
A --> L
A --> M
```

**图表来源**
- [FIRECRACKER_TROUBLESHOOTING.md](file://localmanus-backend/scripts/FIRECRACKER_TROUBLESHOOTING.md#L20-L25)

**章节来源**
- [FIRECRACKER_TROUBLESHOOTING.md](file://localmanus-backend/scripts/FIRECRACKER_TROUBLESHOOTING.md#L1-L50)

## 迁移步骤详解

### 第一步：环境准备

1. **安装 Docker**（仅在线模式需要）
   - 确保 Docker 已正确安装和运行
   - 验证 Docker 版本：`docker --version`

2. **配置环境变量**
   - 在 `.env` 文件中设置沙箱配置
   - 支持本地和在线两种模式切换

### 第二步：配置沙箱模式

#### 本地模式配置

```mermaid
sequenceDiagram
participant Dev as 开发者
participant Config as 配置文件
participant Manager as 沙箱管理器
participant Sandbox as 共享沙箱
Dev->>Config : 设置 SANDBOX_MODE=local
Dev->>Config : 设置 SANDBOX_LOCAL_URL
Config->>Manager : 初始化管理器
Manager->>Sandbox : 测试连接
Sandbox-->>Manager : 返回上下文信息
Manager-->>Dev : 配置完成
```

**图表来源**
- [config.py](file://localmanus-backend/core/config.py#L23-L27)
- [firecracker_sandbox.py](file://localmanus-backend/core/firecracker_sandbox.py#L127-L136)

#### 在线模式配置

```mermaid
flowchart TD
A[启动在线模式] --> B[检查 Docker]
B --> C{Docker 可用?}
C --> |是| D[选择镜像]
C --> |否| E[错误处理]
D --> F{使用中国镜像?}
F --> |是| G[enterprise-public-cn-beijing.cr...]
F --> |否| H[ghcr.io/agent-infra/sandbox]
G --> I[启动容器]
H --> I
I --> J[等待沙箱就绪]
J --> K[返回沙箱信息]
```

**图表来源**
- [firecracker_sandbox.py](file://localmanus-backend/core/firecracker_sandbox.py#L137-L203)

**章节来源**
- [FIRECRACKER_TROUBLESHOOTING.md](file://localmanus-backend/scripts/FIRECRACKER_TROUBLESHOOTING.md#L215-L232)

### 第三步：更新代码集成

#### 技能系统集成

```mermaid
classDiagram
class GenWebSkill {
+create_fullstack_project(project_name, tech_stack, user_id)
+run_shell_command(command, user_id, cwd)
+read_file(file_path, user_id)
+write_file(file_path, content, user_id)
+list_files(directory, user_id)
+start_dev_server(project_dir, user_id, port)
+get_sandbox_info(user_id)
}
class SandboxManager {
+get_sandbox(user_id) SandboxInfo
+get_client(user_id) SandboxClient
+execute_command(user_id, cmd, cwd)
}
class SystemExecutionSkill {
+python_execute(code)
+shell_execute(command)
}
GenWebSkill --> SandboxManager : 使用
SystemExecutionSkill --> SandboxManager : 使用
```

**图表来源**
- [gen_web.py](file://localmanus-backend/skills/gen-web/gen_web.py#L8-L254)
- [system_tools.py](file://localmanus-backend/skills/system-execution/system_tools.py#L6-L78)

**章节来源**
- [gen_web.py](file://localmanus-backend/skills/gen-web/gen_web.py#L15-L76)
- [system_tools.py](file://localmanus-backend/skills/system-execution/system_tools.py#L16-L41)

## 配置管理

### 环境变量配置

| 配置项 | 默认值 | 描述 | 用途 |
|--------|--------|------|------|
| `SANDBOX_MODE` | `local` | 沙箱运行模式 | 选择本地或在线模式 |
| `SANDBOX_LOCAL_URL` | `http://192.168.126.133:8080` | 本地沙箱地址 | 本地模式下的沙箱 URL |
| `USE_CHINA_MIRROR` | `false` | 是否使用中国镜像 | 在线模式下加速镜像下载 |

### 动态配置加载

```mermaid
flowchart LR
A[.env 文件] --> B[dotenv.load_dotenv]
B --> C[os.getenv]
C --> D[config.py]
D --> E[SANDBOX_MODE]
D --> F[SANDBOX_LOCAL_URL]
D --> G[USE_CHINA_MIRROR]
E --> H[SandboxManager 初始化]
F --> H
G --> H
```

**图表来源**
- [config.py](file://localmanus-backend/core/config.py#L1-L27)
- [.env.example](file://localmanus-backend/.env.example#L5-L12)

**章节来源**
- [config.py](file://localmanus-backend/core/config.py#L23-L27)
- [.env.example](file://localmanus-backend/.env.example#L1-L12)

## 技能集成

### Web 项目生成技能

GenWebSkill 是沙箱系统集成的最佳实践示例，展示了如何在技能中使用新的沙箱 API。

#### 核心功能流程

```mermaid
sequenceDiagram
participant Skill as GenWebSkill
participant Manager as SandboxManager
participant Client as SandboxClient
participant Sandbox as 沙箱环境
Skill->>Manager : get_sandbox(user_id)
Manager->>Sandbox : 获取沙箱信息
Sandbox-->>Manager : 返回 SandboxInfo
Manager-->>Skill : 返回沙箱信息
Skill->>Manager : get_client(user_id)
Manager->>Client : 创建客户端
Client->>Sandbox : 执行命令
Sandbox-->>Client : 返回执行结果
Client-->>Skill : 返回结果
Skill->>Skill : 处理结果并返回
```

**图表来源**
- [gen_web.py](file://localmanus-backend/skills/gen-web/gen_web.py#L27-L76)

#### 文件操作示例

```mermaid
flowchart TD
A[用户请求文件操作] --> B[获取沙箱信息]
B --> C[获取沙箱客户端]
C --> D{操作类型}
D --> |读取| E[client.read_file]
D --> |写入| F[client.write_file]
D --> |列出| G[client.list_files]
E --> H[返回文件内容]
F --> I[确认写入成功]
G --> J[返回文件列表]
H --> K[构建响应]
I --> K
J --> K
K --> L[返回给用户]
```

**图表来源**
- [gen_web.py](file://localmanus-backend/skills/gen-web/gen_web.py#L105-L172)

**章节来源**
- [gen_web.py](file://localmanus-backend/skills/gen-web/gen_web.py#L15-L254)

## 测试与验证

### 测试脚本使用

系统提供了完整的测试脚本来验证沙箱功能：

#### 本地模式测试

```mermaid
flowchart TD
A[启动本地模式测试] --> B[初始化 SandboxManager]
B --> C[设置 LOCAL 模式]
C --> D[连接到本地沙箱]
D --> E[获取沙箱信息]
E --> F[执行 shell 命令]
F --> G[文件操作测试]
G --> H[浏览器信息获取]
H --> I[测试完成]
```

**图表来源**
- [test_sandbox.py](file://localmanus-backend/scripts/test_sandbox.py#L13-L84)

#### 在线模式测试

```mermaid
flowchart TD
A[启动在线模式测试] --> B[初始化 SandboxManager]
B --> C[设置 ONLINE 模式]
C --> D[检查 Docker 状态]
D --> E[启动 Docker 容器]
E --> F[等待容器就绪]
F --> G[创建沙箱实例]
G --> H[执行功能测试]
H --> I[清理容器资源]
I --> J[测试完成]
```

**图表来源**
- [test_sandbox.py](file://localmanus-backend/scripts/test_sandbox.py#L85-L141)

**章节来源**
- [test_sandbox.py](file://localmanus-backend/scripts/test_sandbox.py#L1-L191)

## 故障排除

### 常见问题及解决方案

#### 本地沙箱连接失败

```mermaid
flowchart TD
A[连接失败] --> B{检查沙箱状态}
B --> C{沙箱是否运行?}
C --> |否| D[启动沙箱容器]
C --> |是| E{检查端口占用}
E --> F{端口被占用?}
F --> |是| G[查找并终止占用进程]
F --> |否| H{检查网络配置}
G --> H
H --> I{网络正常?}
I --> |否| J[修复网络配置]
I --> |是| K[检查防火墙设置]
K --> L[重新连接测试]
D --> L
J --> L
```

#### Docker 容器启动失败

```mermaid
flowchart TD
A[Docker 启动失败] --> B[检查 Docker 服务]
B --> C{Docker 服务运行?}
C --> |否| D[启动 Docker 服务]
C --> |是| E[检查镜像可用性]
E --> F{镜像可下载?}
F --> |否| G[启用中国镜像]
F --> |是| H[检查磁盘空间]
G --> H
H --> I{磁盘空间充足?}
I --> |否| J[清理磁盘空间]
I --> |是| K[检查端口冲突]
J --> K
K --> L{端口冲突?}
L --> |是| M[修改端口配置]
L --> |否| N[查看详细日志]
M --> O[重试启动]
N --> O
D --> O
O --> P[问题解决]
```

**章节来源**
- [FIRECRACKER_TROUBLESHOOTING.md](file://localmanus-backend/scripts/FIRECRACKER_TROUBLESHOOTING.md#L262-L292)

## 最佳实践

### 性能优化建议

1. **合理选择运行模式**
   - 开发阶段使用本地模式以获得最佳性能
   - 生产环境使用在线模式确保安全隔离

2. **资源管理**
   - 及时清理不再使用的沙箱容器
   - 监控内存和 CPU 使用情况

3. **错误处理**
   - 实现重试机制处理临时性故障
   - 记录详细的日志便于调试

### 安全考虑

1. **容器安全**
   - 使用适当的 seccomp 配置
   - 限制容器权限和资源使用

2. **数据保护**
   - 定期备份重要数据
   - 实施访问控制和审计日志

## 总结

LocalManus 项目的沙箱系统迁移是一个成功的现代化改造案例，展现了从传统虚拟机方案到现代容器化沙箱架构的完整转型过程。

### 主要成就

1. **架构升级** - 从 Firecracker VM 升级到 agent-infra/sandbox
2. **功能增强** - 添加浏览器自动化、VSCode、Jupyter 等高级功能
3. **易用性提升** - 简化的部署流程和配置管理
4. **灵活性改进** - 支持多种运行模式满足不同场景需求

### 未来展望

- 继续扩展沙箱功能，支持更多开发工具集成
- 优化性能表现，减少容器启动时间
- 增强监控和诊断能力
- 扩展多租户支持和资源隔离

通过这次迁移，LocalManus 项目为用户提供了更加丰富、灵活和可靠的开发体验，为未来的功能扩展奠定了坚实的基础。

**章节来源**
- [FIRECRACKER_TROUBLESHOOTING.md](file://localmanus-backend/scripts/FIRECRACKER_TROUBLESHOOTING.md#L247-L304)